@AGENTS.md

# 프로젝트 규칙 (CLAUDE.md)

이 문서는 `my-app` 작업 시 지켜야 할 규칙입니다. 기획 내용은 [PRD.md](PRD.md)를 기준으로 합니다.

## 프로젝트 개요

국가별 대리점의 실적·forecast를 기간별·장비별로 한눈에 확인할 수 있는 대시보드 (PoC 단계).
자세한 배경·목표·기능은 PRD.md 참고.

## 기술 스택 (고정)

- **프레임워크**: Next.js (App Router), TypeScript, `app/` 디렉터리 사용 (src 디렉터리 없음)
- **스타일**: Tailwind CSS
- **린트**: ESLint (`eslint-config-next`)
- **데이터베이스**: Vercel Postgres
- **챗봇 AI**: OpenAI(GPT) API
- **로그인**: Supabase Auth (이메일/비밀번호, `@supabase/ssr`)
- **배포**: Vercel
- **import 경로**: `@/*` 별칭 사용 (tsconfig.json 기준)

이 스택 외의 새로운 라이브러리·프레임워크·DB를 임의로 추가하지 말 것. 꼭 필요하면 먼저 사용자에게 확인.
Next.js는 PRD에서 고정한 기술이므로, 다른 프레임워크로 바꾸거나 마이그레이션하자는 제안을 하지 않는다. 배포는 항상 Vercel을 사용한다.

## 이번 PoC 범위 (PRD.md 6번 기준)

**만든다**
- 실적·forecast 조회 대시보드 (국가·기간·장비 조건, 실제 수치가 표시되는 그래프)
- 챗봇을 통한 실적·forecast 데이터 입력
- 기존 엑셀 자료를 초기에 한 번에 올리는 엑셀 업로드 기능
- Supabase Auth(이메일/비밀번호) 로그인. 가입은 누구나 이메일로 가능(공개 가입). 담당자·관리자 권한 차이는 두지 않는다.

**만들지 않는다 (다음 단계로 미룸)**
- 로그인한 사용자 간 권한 차등(역할/직급별 접근 제한)
- ERP 등 외부 시스템과의 자동 데이터 연동
- 별도의 수정/삭제 화면 (같은 국가·기간·장비로 재입력 시 자동 덮어쓰기만 지원)
- 알림/리포트 자동 발송
- 모바일 전용 화면 최적화

범위 밖 기능을 임의로 추가 구현하지 말 것. 필요하다고 판단되면 먼저 사용자에게 제안하고 확인받을 것.

## 챗봇(데이터 입력) 관련 규칙

- 국가·기간·장비·수치 중 하나라도 빠지면 저장하지 말고 반드시 되물어서 확인한다.
- 숫자가 아닌 값, 음수 등 비정상 수치는 저장 전에 사용자에게 확인받는다.
- 같은 국가·기간·장비 조합의 데이터가 이미 있으면 기존 값을 덮어쓰고, 덮어쓴다는 사실을 사용자에게 알린다.
- 국가·장비명은 미리 정해둔 목록이 아니라 자유 입력을 그대로 저장한다.

## 보안 · 환경 변수

- API 키, DB 접속 정보 등 모든 민감한 값은 반드시 `.env`에만 두고 코드에 하드코딩하지 않는다.
- `.env`, `node_modules` 등은 `.gitignore`에 등록해 두고 절대 git에 커밋하지 않는다.
- 코드, 커밋 메시지, 로그, 응답 어디에도 `.env`에 들어있는 실제 키 값을 그대로 출력하지 않는다.
- 이 프로젝트는 사내 전용 자료를 다루며, 접근은 Supabase Auth 로그인(이메일/비밀번호)으로 제한한다. `middleware.ts`가 미로그인 사용자를 `/login`으로 보낸다. 가입은 공개(누구나 이메일로 가입 가능)이며, 로그인한 사용자 간 권한 차이는 없다.
- Supabase 관련 환경변수: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` (둘 다 클라이언트에 노출되는 공개용 키). `SUPABASE_ACCESS_TOKEN`은 Supabase CLI 인증 전용이며 절대 클라이언트 코드에 쓰지 않는다.
- 외부 서비스 인증이 필요할 때는 사용자에게 토큰 값을 묻거나 채팅에 출력하지 않고, `.env`에 있는 값을 읽어서 사용한다.
  - 예: Supabase 작업이 필요하면 Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 인증한다.
  - 예: Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로 인증한다.

## 코딩 컨벤션

- 모든 설명과 주석은 한국어로 작성한다.
- TypeScript strict 모드를 유지하고, `any` 사용을 피한다.
- 새 화면/기능은 App Router 규칙(`app/` 하위 폴더 라우팅)을 따른다.
- 스타일링은 Tailwind 유틸리티 클래스를 우선 사용한다.
- 커밋 전 `npm run lint`로 확인한다.
- 과도한 추상화나 이번 범위에 없는 기능을 미리 만들지 않는다 (PoC 단계이므로 필요한 것만 최소로 구현).
- 새 파일은 `my-app` 폴더 안에만 만든다. 상위 폴더나 다른 위치에 파일을 만들지 않는다.
- 코드를 변경하면 무엇을 왜 바꿨는지 한 줄로 알려준다.

## 파일 삭제 규칙

- 파일을 지워야 할 때는 바로 삭제하지 않고, `trash-can` 폴더를 만들어 그 안으로 옮겨둔다.
- 실제 삭제는 사용자가 직접 확인한 뒤 진행한다.

## 서브에이전트 활용

- 이미 설치된 서브에이전트가 작업에 맞으면 적극적으로 활용한다.
