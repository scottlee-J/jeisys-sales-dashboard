# 국가별 대리점 실적·Forecast 대시보드 서비스 기획서 (PRD)

- 작성자: 이호석
- 작성일: 2026-09-14

## 1) 추진 배경 (문제 정의)
국가별 대리점의 실적과 forecast(예측) 데이터는 존재하지만, 기간별·장비별로 구분해서 한눈에 비교하거나 추이를 파악할 수 있는 형태로 정리되어 있지 않다. 이 때문에 각 국가 담당자와 관리자는 실적 현황을 확인하는 데도 시간이 걸리고, 국가 간 비교나 이상 징후를 빠르게 알아차리기 어렵다.

## 2) 현행 방식과 한계
현재는 국가별·대리점별 실적과 forecast를 엑셀 등 개별 파일로 취합해 관리하고 있으며, 기간별·장비별로 비교하려면 담당자가 매번 데이터를 수작업으로 정리해야 한다. 이 과정에서 국가마다 자료 형식과 갱신 주기가 달라 데이터를 한 화면에서 비교하기 어렵고, 최신 실적을 확인하는 데도 시간이 걸린다.

## 3) 목표와 기대효과 (성공 기준)
- 국가별·대리점별·기간별·장비별 현황 및 forecast(예상 수치)를 실제 수치가 함께 표시되는 그래프로 한눈에 조회할 수 있다.
- 실적 및 forecast를 대시보드 화면에서 **3초 이내**에 조회할 수 있다.
- 실적 확인에 걸리는 시간을 기존 수작업 대비 **50% 이상 단축**한다.
- 국가 담당자·관리자의 반복 데이터 취합 문의를 **월 10건 이하**로 줄인다.

## 4) 사용자와 이용 흐름
- **사용자**: 각 국가별 담당자, 각 국가별 관리자 (Supabase 이메일/비밀번호 계정으로 개별 로그인. 누구나 이메일로 회원가입 가능. 담당자·관리자 간 권한 차이 없이 둘 다 조회·입력 가능)
- **이용 흐름**:
  대시보드 접속 → 조건을 세부적으로 선택하지 않아도 전체(또는 상위 단위) 실적·forecast가 기본으로 노출 → 국가·기간·장비 등 원하는 조건을 선택해 범위를 좁히거나, 여러 조건(예: 여러 국가·여러 기간)을 실제 수치가 표시된 그래프 안에서 함께 비교 → 필요 시 조건을 바꿔가며 반복 확인

## 5) 주요 기능 (must / nice로 구분)

**Must**
1. **실적·Forecast 조회 대시보드**
   국가·기간·장비별 실적 및 forecast를 실제 수치가 함께 표시되는 그래프로 보여준다. 조건을 세부 선택하지 않아도 기본적으로 전체(상위 단위) 데이터가 노출되어야 한다.
   - *AI가 지킬 규칙*: 국가·기간·장비 조건이 하나라도 비어 있으면 해당 조건은 "전체"로 간주해 상위 단위 합계를 표시할 것. 그래프에는 막대·점 등 시각 요소뿐 아니라 실제 수치 값도 함께 표기할 것. 절대 빈 화면이나 에러를 띄우지 말 것.

2. **챗봇을 통한 데이터 입력**
   담당자가 챗봇과의 대화로 실적·forecast 데이터를 입력할 수 있다. 국가·장비는 미리 정해둔 목록이 아니라, 담당자가 챗봇에 입력한 이름을 그대로 저장하는 자유 입력 방식으로 시작한다.
   - *AI가 지킬 규칙*: 국가·기간·장비·수치 중 하나라도 빠지면 저장하지 말고 반드시 되물어서 확인할 것. 숫자가 아닌 값이나 음수 등 비정상 수치는 저장 전에 반드시 사용자에게 확인받을 것. 이미 같은 국가·기간·장비 조합의 값이 있으면 기존 값을 새 값으로 덮어쓰고, 덮어쓴다는 사실을 사용자에게 알릴 것.

**Nice to have**
- 기존 엑셀 자료를 초기에 한 번에 올릴 수 있는 엑셀 업로드 기능 (초기 데이터 이관용)
- 여러 국가/기간/장비를 하나의 그래프 안에서 나란히 비교하는 다중 조건 비교 뷰
- 그래프 데이터를 엑셀로 다운로드하는 기능

## 6) 범위와 비범위

**이번에 만들 것**
- 국가·기간·장비별 실적/forecast 조회 대시보드 (실제 수치가 표시되는 그래프)
- 챗봇을 통한 실적/forecast 데이터 입력
- 기존 엑셀 자료를 처음에 한 번에 올리는 엑셀 업로드 기능 (초기 데이터 이관용)

**이번에 절대 만들지 않을 것**
- 로그인/권한 관리 (국가·직급별 접근 제한 등)
- 실적 데이터 자동 수집·연동 (ERP, 기존 시스템과의 자동 연동)
- 별도의 수정/삭제 화면 (같은 조건 재입력 시 챗봇이 자동으로 덮어쓰는 것 외의 수정·삭제 기능은 다음 단계로 미룸)
- 알림/리포트 자동 발송 기능
- 모바일 전용 화면 최적화

## 7) 보안·개인정보 검토
- **공개등급**: 사내 실적/forecast 데이터로, 외부 공개 불가가 원칙(사내 담당자·관리자만 접근 가능한 내부용 자료). Supabase Auth(이메일/비밀번호)로 로그인해야 접근 가능하며, 가입은 누구나 이메일로 할 수 있게 열어뒀다(사내 전용 원칙과는 다소 배치되므로, 필요하면 추후 이메일 도메인 제한 등으로 좁히는 것을 고려할 수 있다).
- **개인정보 여부**: 개인정보(이름, 연락처 등)는 다루지 않으며, 국가·대리점·장비 단위의 실적 수치만 취급한다.
- **API 키 관리**: 챗봇(OpenAI GPT) 연동에 필요한 API 키는 소스 코드에 직접 넣지 않고 환경변수(.env)로 관리하며, .gitignore에 등록해 저장소(git)에 커밋되지 않도록 한다.

## 8) 기술 스택
- **Next.js**: 화면(프런트엔드)과 서버 쪽 코드(API)를 한 프로젝트에서 함께 처리한다.
- **고정 이유**: STEP 2에서 `my-app`에 이미 설치했고, Part 5에서 Vercel로 배포할 예정이라 그대로 이어지게 하기 위함.
- **데이터베이스**: Vercel Postgres — Vercel 배포와 연결이 간편하고, 정식 DB로 데이터를 안전하게 저장하기 위함.
- **챗봇 AI**: OpenAI(GPT) API — 데이터 입력용 챗봇 대화 처리에 사용.
