# 개발 계획 (PLAN.md)

이 문서는 지금까지 만든 것과 앞으로 할 일을 정리한 작업 계획서입니다.
"무엇을 왜 만드는가"는 [PRD.md](PRD.md), "어떻게 만드는가(규칙)"는 [CLAUDE.md](CLAUDE.md),
"어떻게 쓰는가"는 [README.md](README.md)를 봅니다.

## 1. 현재 상태

PoC 1차 구현과 배포까지 끝난 상태입니다.

| 영역 | 상태 | 비고 |
| --- | --- | --- |
| 조건별 조회 (영업팀 → 국가 → 거래처 → 장비 → 품목) | 완료 | 고르지 않은 조건은 전체 합계 |
| 기간별 그래프 (연/반기/분기/월), 달성률, 전년 동기 대비 | 완료 | 외부 차트 라이브러리 없이 SVG 로 직접 그림 |
| 요약 카드 | 완료 | 선택한 기간 단위의 마지막 기간 기준, 기준 기간을 카드에 표기 |
| 챗봇 데이터 입력 | 완료 | 빠진 항목은 되묻고, 같은 조건은 덮어쓰며 알림 |
| 챗봇 조회·순위 | 완료 | 순위·합계는 AI 가 아니라 서버가 직접 집계 |
| PO(발주서) PDF 업로드 | 완료 | 미리보기에서 장비·품목·수량·단가를 고친 뒤 실적에 반영 |
| 엑셀(CSV) 내보내기 | 완료 | 지금 고른 조건의 원본 데이터 |
| 로그인·권한 | 완료 | Supabase Auth, 관리자는 전체 / 일반 사용자는 소속 팀만 |
| 데이터 저장 | 완료 | Supabase Postgres (`public.records`), 배포 환경에서도 저장 가능 |
| 배포 | 완료 | Vercel |

## 2. 알려진 한계

1. **거래처별 미확정 데이터** — 원본 자료에서 담당 거래처를 특정할 수 없는 매출이 남아 있습니다.
   임의로 배분하지 않고 "미확정"으로 두되 화면에는 보이지 않게 처리했고, 지금은 대만만 실제 거래처로 정리했습니다.
2. **PO 중복 반영 방지 없음** — 같은 PO 를 두 번 올리면 실적이 두 번 더해집니다. PO 번호를 기억하지 않습니다.
3. **같은 품목이 여러 줄인 PO** — 미리보기의 "반영 후 실적"이 각 줄을 따로 계산해서, 실제 최종값보다 작게 보일 수 있습니다.
4. **권한이 없는 계정** — 소속 팀이 지정되지 않은 계정은 전체 데이터가 보입니다(열려 있는 쪽으로 동작).
5. **전년도 데이터는 데모용** — 비교 기능을 보여 주기 위한 임시 값이며, 실제 실적이 아닙니다.
6. **품목 세분화** — 소모품 품목 분류를 정리해 두었지만, 기존 데이터에는 아직 반영되지 않았습니다(데이터 재입력 시 적용).

## 3. 다음 할 일

### 3.1 데이터 정확도 (우선)

- 실제 실적 데이터를 다시 올리면서 정리된 품목 분류를 적용한다.
- 대만 외 국가(홍콩·태국·인도네시아·싱가포르·말레이시아)의 "거래처별 미확정" 매출을 담당자 확인을 받아 거래처에 배정한다.
- 데모용 전년도 데이터를 실제 값으로 교체한다.

### 3.2 운영 안전

- 소속 팀이 없는 계정은 전체가 아니라 아무것도 보이지 않도록 바꾼다(막히는 쪽이 기본).
- 회원가입을 사내 이메일 도메인으로 제한하거나, 관리자가 계정을 만들어 주는 방식으로 바꾼다.

### 3.3 편의 기능

- PO 번호를 저장해 같은 PO 의 중복 반영을 막는다.
- 미리보기에서 같은 장비·품목인 줄을 합쳐 보여 준다.
- 합계가 0 인 줄은 기본적으로 반영에서 제외하고, 필요할 때만 켜게 한다.

## 4. 이번 단계에서 하지 않는 것

- ERP 등 외부 시스템과의 자동 연동
- 별도의 수정·삭제 화면 (같은 조건 재입력으로 덮어쓰는 방식 유지)
- 알림·리포트 자동 발송
- 모바일 전용 화면 최적화
- PO 를 폴더에 넣어 두면 자동으로 읽어 오는 기능 (검토만 했고 구현하지 않음)

## 5. 참고

- 기술 스택과 실행 방법: [README.md](README.md)
- 작업 규칙(한국어 주석, 스택 고정, 파일 삭제 절차 등): [CLAUDE.md](CLAUDE.md)
- 접근은 로그인한 계정만 가능하며, 데이터 저장 방식을 바꿀 때 고쳐야 할 곳은 `lib/store.ts` 한 곳입니다.
